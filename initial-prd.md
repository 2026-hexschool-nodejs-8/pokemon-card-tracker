> 這份 Mini PRD 是給學生用的 LV3 專案需求。老師提出產品需求，學生自行分組、拆任務、開發 MVP，最後在成果發表時分享產品思考、資料流設計、技術取捨、AI 協作方式與遇到的困難。

---

## 一、文件定位

| 項目   | 說明                                       |
| ---- | ---------------------------------------- |
| 文件類型 | Mini PRD / 教學用產品需求文件                     |
| 專案型態 | Node.js Side Project                     |
| 難度   | LV3：排程任務、爬蟲 / API、資料庫、價格歷史               |
| 開發方式 | 學生自行分組、拆任務、完成 MVP                        |
| 預計時程 | 30 天                                     |
| 成果發表 | 2026-08-21                               |
| 評估重點 | 產品觀察、資料流設計、排程穩定度、資料庫設計、錯誤處理、AI 協作能力、表達能力 |

這份文件不是要你們做出完整交易平台，而是練習把「卡牌查價」這件看似簡單的需求，拆成一個真的可以持續運作的資料服務。

---

## 二、產品背景

請你們開發一個 **寶可夢卡牌價格追蹤器**。

使用者可以在前台查詢卡牌，看到該卡目前的參考價格與歷史價格變化。系統不是等使用者按下查詢才臨時爬資料，而是會定期執行排程，透過 API 與爬蟲抓取卡價，並把每次查到的價格存進資料庫，形成可追蹤的價格歷史。

這題的重點是：

- 看起來只是「查一張卡多少錢」，實際上需要穩定的資料流程
- 外部資料來源可能失敗、變慢、改版或被限制
- 價格需要標準化，例如幣別、品相、鑑定分數、來源時間
- 系統要能保留歷史，而不是只覆蓋最新價格
- 前台要讓使用者看懂「現在多少」與「最近怎麼變」

---

## 三、產品目標

這個專案要完成一個可以 Demo 的卡價追蹤服務：

- 管理者可以建立要追蹤的卡牌資料
- 系統可以透過排程定期更新卡價
- 系統至少支援一個 API 來源與一個爬蟲來源
- 系統會把每次抓到的價格存成歷史快照
- 使用者可以在前台搜尋卡牌
- 使用者可以查看卡牌最新價格與歷史價格
- 管理者可以查看排程執行狀態與錯誤紀錄
- 小組能說清楚使用哪些 AI 服務加速開發，以及自己做了哪些判斷

---

## 四、成功指標

| 指標 | 達成標準 |
|---|---|
| 排程可執行 | 系統可以定期或手動觸發卡價更新任務 |
| 外部來源可串接 | 至少有一個 API adapter 與一個 crawler adapter 能取得價格資料 |
| 歷史有保存 | 每次更新都會新增價格快照，不只覆蓋最新價格 |
| 前台可查詢 | 使用者可以搜尋卡牌並看到最新價格 |
| 趨勢可理解 | 使用者可以看到歷史價格列表或簡單趨勢圖 |
| 後台可監控 | 管理者可以看到最近一次更新時間、成功 / 失敗狀態與錯誤訊息 |
| 錯誤可處理 | 外部來源失敗時，不會讓整個服務崩潰，並會留下 log |
| README 可使用 | 同學照 README 可以安裝、啟動、建立測試資料、跑一次排程、看前台結果 |
| AI 協作可說明 | 可以說出使用哪些 AI 服務、加速了哪些開發環節、哪些地方仍由小組自己判斷 |

---

## 五、非目標

這些事情不是 MVP 階段的重點。除非主流程已經穩定，否則不要一開始就做。

- 不需要做真實交易、下單、付款或出貨
- 不需要做完整會員系統
- 不需要抓全世界所有卡牌
- 不需要支援所有語言、所有版本、所有品相
- 不需要做到投資建議或買賣建議
- 不需要繞過登入、付費牆、驗證碼或反爬機制
- 不需要把爬到的外部頁面內容完整複製到自己的網站
- 不需要一開始就做高頻即時報價

---

## 六、核心產品問題

很多人會直覺覺得：

> 卡價查詢不就是按一下，然後顯示結果嗎？

LV3 的重點是讓你們理解，真正的產品流程比較像這樣：

```txt
建立追蹤卡牌
  ↓
設定資料來源
  ↓
排程定期啟動
  ↓
呼叫 API / 爬蟲抓價格
  ↓
清洗價格資料
  ↓
寫入價格快照
  ↓
更新卡牌最新價格
  ↓
前台顯示目前價格與歷史趨勢
  ↓
後台檢查任務成功或失敗
```

你們不是只做一個按鈕，而是做一條能反覆運作的資料管線。

---

## 七、角色與權限

MVP 可以先簡化權限。如果時間有限，管理者可以用固定後台入口，不一定要做完整登入。

| 角色 | 可以做什麼 | 不能做什麼 |
|---|---|---|
| 訪客 / 使用者 | 搜尋卡牌、查看最新價格、查看歷史價格 | 不能新增卡牌，不能觸發排程 |
| 管理者 | 新增 / 編輯追蹤卡牌、設定來源、手動觸發更新、查看任務 log | 不應該修改歷史快照內容，除非有清楚的修正紀錄 |

建議做法：

- LV3 基準版：可以先用簡單 admin token 保護後台 API
- 加分版：做管理者登入與 session / JWT

---

## 八、目標使用者與情境

你們可以想像使用者是：

- 想追蹤收藏卡價格的寶可夢卡牌玩家
- 想觀察高價卡走勢的收藏者
- 想知道某張卡最近有沒有漲跌的人
- 想管理自己關注清單的卡牌投資者

核心任務：

> 我想知道某張寶可夢卡現在大概多少錢，也想知道它最近的價格變化，而不是每次都自己去不同網站查。

---

## 九、使用者故事

### MVP 使用者故事

| ID | 使用者故事 | 驗收條件 |
|---|---|---|
| US-01 | 作為一位管理者，我想新增一張要追蹤的卡牌，讓系統知道要查哪張卡。 | 後台可以新增卡名、卡號、版本、語言、鑑定狀態與來源設定。 |
| US-02 | 作為一位管理者，我想手動執行一次價格更新，方便測試排程流程。 | 後台按下更新或呼叫 API 後，系統會建立一筆 job，並嘗試抓取價格。 |
| US-03 | 作為系統，我想定期執行價格更新，讓卡價不需要人工每天更新。 | 使用 `node-cron` 或等效方式，每天至少執行一次更新任務。 |
| US-04 | 作為系統，我想透過 API 來源取得價格，避免全部依賴網頁爬蟲。 | 至少有一個 API adapter，能解析來源回應並轉成系統內部格式。 |
| US-05 | 作為系統，我想透過爬蟲來源取得價格，練習處理外部 HTML 資料。 | 至少有一個 crawler adapter，能解析頁面中的價格資料並轉成系統內部格式。 |
| US-06 | 作為系統，我想把每次查到的價格存成歷史快照，之後可以看趨勢。 | 每次更新成功時，都會新增 `PriceSnapshot`，包含價格、幣別、來源、抓取時間。 |
| US-07 | 作為一位使用者，我想搜尋卡牌並看到最新價格。 | 前台可以搜尋卡名或卡號，並顯示最新價格、來源與最後更新時間。 |
| US-08 | 作為一位使用者，我想看到卡牌的歷史價格。 | 卡牌詳情頁可以看到歷史價格表格或簡單趨勢圖。 |
| US-09 | 作為一位管理者，我想知道排程有沒有失敗。 | 後台可以看到最近 job 狀態、錯誤訊息與執行時間。 |

### 打磨與加分使用者故事

| ID | 使用者故事 | 可以怎麼做 |
|---|---|---|
| US-10 | 作為一位使用者，我想建立自己的關注清單。 | 加入 watchlist，讓使用者追蹤常看的卡牌。 |
| US-11 | 作為一位收藏者，我想比較不同來源價格。 | 顯示各來源最新價格與平均價格。 |
| US-12 | 作為一位使用者，我想看到 7 天 / 30 天漲跌幅。 | 根據歷史快照計算漲跌金額與百分比。 |
| US-13 | 作為一位管理者，我想控制每個來源的抓取頻率。 | 在 `PriceSource` 設定 rate limit 或 refresh interval。 |
| US-14 | 作為一位管理者，我想知道資料是否異常。 | 價格突然變成 0、Infinity、或漲跌過大時標記為 suspicious。 |

---

## 十、MVP 範圍

MVP 基準版請先固定成：

> 管理者新增 3-5 張卡牌 → 設定 API 與爬蟲來源 → 手動觸發一次更新 → 排程每日更新 → 價格寫入資料庫 → 前台可以搜尋卡牌並看到最新價格與歷史價格。

| # | 功能 | 說明 |
|---|---|---|
| 1 | 卡牌資料管理 | 管理者可以新增要追蹤的卡牌 |
| 2 | 價格來源設定 | 每張卡可以設定 API 來源與爬蟲來源 |
| 3 | 手動更新 | 管理者可以手動觸發一次卡價更新 |
| 4 | 排程更新 | 系統每天自動跑一次價格同步 |
| 5 | API adapter | 至少串接一個 API 型資料來源 |
| 6 | Crawler adapter | 至少串接一個 HTML 爬蟲型資料來源 |
| 7 | 價格快照 | 每次更新成功都新增一筆價格歷史 |
| 8 | Job log | 每次更新任務都記錄成功 / 失敗與錯誤訊息 |
| 9 | 前台搜尋 | 使用者可以搜尋卡名或卡號 |
| 10 | 價格詳情 | 使用者可以看到最新價格、最後更新時間與歷史價格 |

---

## 十一、功能規格

| ID | 功能 | 規格 | 優先級 |
|---|---|---|---|
| FR-01 | 服務狀態檢查 | 提供 `GET /health`，回傳服務是否正常 | Must |
| FR-02 | 新增卡牌 | 管理者可新增卡名、卡號、版本、語言、鑑定狀態 | Must |
| FR-03 | 卡牌列表 | 前台可列出卡牌，支援關鍵字搜尋 | Must |
| FR-04 | 卡牌詳情 | 前台可查看單張卡牌最新價格與歷史價格 | Must |
| FR-05 | 來源設定 | 管理者可為卡牌設定來源類型、來源網址或來源代碼 | Must |
| FR-06 | API 抓價 | 實作一個 API adapter，將外部回應轉成標準價格格式 | Must |
| FR-07 | HTML 爬蟲抓價 | 實作一個 crawler adapter，解析 HTML 中的價格資料 | Must |
| FR-08 | 價格標準化 | 將價格轉成數字、保留幣別、來源、抓取時間 | Must |
| FR-09 | 價格快照 | 每次抓價成功新增一筆歷史快照 | Must |
| FR-10 | 最新價格更新 | 卡牌列表可顯示最新可用價格與最後更新時間 | Must |
| FR-11 | 手動觸發 job | 管理者可手動觸發單張或全部卡牌更新 | Must |
| FR-12 | 定時排程 | 使用 `node-cron` 或等效方式每日自動更新 | Must |
| FR-13 | Job log | 記錄 job 開始時間、結束時間、狀態、錯誤訊息 | Must |
| FR-14 | 來源失敗處理 | 單一來源失敗時，不影響其他來源繼續執行 | Must |
| FR-15 | 趨勢圖 | 前台用折線圖呈現價格歷史 | Should |
| FR-16 | 7 / 30 天漲跌 | 根據歷史快照計算漲跌幅 | Should |
| FR-17 | Watchlist | 使用者可收藏關注卡牌 | Could |
| FR-18 | 異常價格標記 | 對 0、Infinity、超大跳動價格標記為異常 | Could |

---

## 十二、使用者流程

### 前台使用者流程

```txt
使用者打開前台
  ↓
搜尋卡名或卡號
  ↓
看到卡牌列表
  ↓
點進卡牌詳情
  ↓
查看最新價格、來源與最後更新時間
  ↓
查看歷史價格表格或趨勢圖
```

### 管理者流程

```txt
管理者打開後台
  ↓
新增要追蹤的卡牌
  ↓
設定 API 來源與爬蟲來源
  ↓
手動觸發一次更新測試
  ↓
查看 job log 是否成功
  ↓
確認前台出現最新價格與歷史價格
```

### 系統排程流程

```txt
cron 到達指定時間
  ↓
建立 PriceFetchJob
  ↓
讀取所有啟用中的卡牌與來源
  ↓
依序呼叫 API adapter / crawler adapter
  ↓
清洗與標準化價格
  ↓
寫入 PriceSnapshot
  ↓
更新 Card latest price summary
  ↓
寫入 job 成功 / 失敗狀態
```

---

## 十三、建議 API

你們可以依照自己的設計調整，但 MVP 至少要支援下列類型。

### Public

| Method | Path | 權限 | 說明 |
|---|---|---|---|
| `GET` | `/health` | 公開 | 確認服務正常 |
| `GET` | `/cards` | 公開 | 取得卡牌列表，支援 `keyword`、`language`、`grade` |
| `GET` | `/cards/:id` | 公開 | 取得卡牌詳情與最新價格 |
| `GET` | `/cards/:id/prices` | 公開 | 取得卡牌歷史價格，可支援 `from`、`to`、`source` |

### Admin Cards

| Method | Path | 權限 | 說明 |
|---|---|---|---|
| `POST` | `/admin/cards` | 管理者 | 新增卡牌 |
| `PATCH` | `/admin/cards/:id` | 管理者 | 編輯卡牌 |
| `DELETE` | `/admin/cards/:id` | 管理者 | 停用卡牌 |
| `POST` | `/admin/cards/:id/sources` | 管理者 | 新增價格來源 |
| `PATCH` | `/admin/sources/:id` | 管理者 | 編輯價格來源 |

### Admin Jobs

| Method | Path | 權限 | 說明 |
|---|---|---|---|
| `POST` | `/admin/jobs/price-sync` | 管理者 | 手動觸發全部卡牌更新 |
| `POST` | `/admin/cards/:id/price-sync` | 管理者 | 手動觸發單張卡牌更新 |
| `GET` | `/admin/jobs` | 管理者 | 查看 job 列表 |
| `GET` | `/admin/jobs/:id` | 管理者 | 查看 job 詳情與錯誤 |

---

## 十四、資料規格

LV3 建議使用 PostgreSQL / MySQL / SQLite 搭配 ORM。因為這題的核心就是價格歷史，請不要只用瀏覽器 `localStorage`。如果一開始想降低難度，可以用 SQLite，但資料表關係要設計清楚。

### Card

| 欄位 | 類型 | 說明 |
|---|---|---|
| `id` | string / uuid | 卡牌 ID |
| `name` | string | 卡牌名稱 |
| `cardNumber` | string | 卡號，例如 `208/XY-P` |
| `setName` | string | 系列或彈別 |
| `language` | string | 語言，例如 `ja`、`en`、`zh` |
| `condition` | string | 品相，例如 raw、PSA10、BGS10 |
| `imageUrl` | string | 卡牌圖片網址，可選 |
| `isActive` | boolean | 是否啟用追蹤 |
| `latestPrice` | number | 最新參考價格，可由快照同步 |
| `latestCurrency` | string | 最新價格幣別 |
| `lastFetchedAt` | datetime | 最後成功更新時間 |
| `createdAt` | datetime | 建立時間 |
| `updatedAt` | datetime | 更新時間 |

### PriceSource

| 欄位 | 類型 | 說明 |
|---|---|---|
| `id` | string / uuid | 來源 ID |
| `cardId` | string | 對應卡牌 |
| `type` | string | `api` 或 `crawler` |
| `provider` | string | 來源名稱，例如 `sourceA` |
| `url` | string | 爬蟲來源網址 |
| `externalId` | string | API 來源 ID，可選 |
| `currency` | string | 預期幣別 |
| `isActive` | boolean | 是否啟用 |
| `lastSuccessAt` | datetime | 最後成功時間 |
| `lastError` | string | 最後錯誤訊息 |

### PriceSnapshot

| 欄位 | 類型 | 說明 |
|---|---|---|
| `id` | string / uuid | 快照 ID |
| `cardId` | string | 對應卡牌 |
| `sourceId` | string | 對應來源 |
| `provider` | string | 來源名稱 |
| `price` | number | 價格數字 |
| `currency` | string | 幣別 |
| `priceTwd` | number | 換算台幣，可選 |
| `rawText` | string | 原始價格文字，可選，例如 `$123.45` |
| `fetchedAt` | datetime | 抓取時間 |
| `isSuspicious` | boolean | 是否疑似異常 |
| `createdAt` | datetime | 寫入時間 |

### PriceFetchJob

| 欄位 | 類型 | 說明 |
|---|---|---|
| `id` | string / uuid | Job ID |
| `triggerType` | string | `manual` 或 `cron` |
| `status` | string | `running`、`success`、`partial_success`、`failed` |
| `startedAt` | datetime | 開始時間 |
| `finishedAt` | datetime | 結束時間 |
| `totalSources` | number | 這次預計更新來源數 |
| `successCount` | number | 成功來源數 |
| `failedCount` | number | 失敗來源數 |
| `errorMessage` | string | 整體錯誤摘要 |

### PriceFetchLog

| 欄位 | 類型 | 說明 |
|---|---|---|
| `id` | string / uuid | Log ID |
| `jobId` | string | 對應 Job |
| `cardId` | string | 對應卡牌 |
| `sourceId` | string | 對應來源 |
| `status` | string | `success` 或 `failed` |
| `message` | string | 成功摘要或錯誤訊息 |
| `durationMs` | number | 執行耗時 |
| `createdAt` | datetime | 建立時間 |

---

## 十五、來源 Adapter 設計

請不要把每個來源的抓取邏輯全部塞在 controller 裡。建議用 adapter 概念，把不同來源包成相同輸出格式。

### 標準輸出格式

```ts
type PriceResult = {
  provider: string;
  price: number;
  currency: string;
  rawText?: string;
  fetchedAt: string;
};
```

### API adapter 範例

```txt
呼叫外部 API
  ↓
確認 HTTP 狀態
  ↓
解析 JSON
  ↓
取出價格欄位
  ↓
轉成 PriceResult
```

### Crawler adapter 範例

```txt
抓取來源網頁 HTML
  ↓
用 cheerio 解析 DOM
  ↓
找出價格文字
  ↓
清理貨幣符號、逗號、空白
  ↓
轉成 PriceResult
```

### 最低要求

- 至少一個 API adapter
- 至少一個 crawler adapter
- adapter 失敗時要丟出可理解錯誤
- controller / scheduler 不應該知道每個來源的 HTML 細節

---

## 十六、排程與資料流程

### 建議排程策略

MVP 可以使用 `node-cron`：

```txt
每天 02:00 執行一次全部卡牌更新
```

也必須提供手動觸發 API，讓成果發表時可以不用等排程時間。

### 排程注意事項

| 問題 | 建議處理 |
|---|---|
| 同時間觸發兩次更新 | 如果已有 running job，可以拒絕或排隊 |
| 單一來源失敗 | 記錄失敗，繼續跑其他來源 |
| 抓到空價格 | 不寫入快照，記錄錯誤 |
| 抓到非數字價格 | 不寫入快照，記錄錯誤 |
| 抓到 Infinity / NaN | 不寫入快照，標記錯誤 |
| 價格異常暴漲暴跌 | 可先寫入但標記 `isSuspicious` |
| 來源太慢 | 設定 timeout，例如 10 秒 |
| 來源被限制 | 顯示錯誤，不要無限重試 |

---

## 十七、前台畫面需求

需要有前台畫面可以操作，不只提供 API。

### 卡牌列表頁

至少包含：

- 搜尋框
- 卡牌名稱
- 卡號 / 系列
- 語言 / 鑑定狀態
- 最新價格
- 最後更新時間
- 進入詳情按鈕

### 卡牌詳情頁

至少包含：

- 卡牌基本資料
- 最新價格
- 來源名稱
- 最後更新時間
- 歷史價格表格
- 簡單趨勢圖或折線圖

### 後台管理頁

至少包含：

- 新增卡牌表單
- 來源設定表單
- 手動更新按鈕
- 最近 job 狀態
- 最近錯誤訊息

---

## 十八、錯誤處理與邊界情境

| 情境 | 預期處理 |
|---|---|
| 搜尋不到卡牌 | 顯示空狀態，不要顯示系統錯誤 |
| 卡牌沒有任何價格 | 顯示尚未更新，提供最後 job 狀態 |
| API 來源回傳 429 | 記錄 rate limit 錯誤，這次來源失敗 |
| API 來源格式改變 | 記錄解析失敗，不寫入錯誤價格 |
| 爬蟲頁面改版 | 記錄找不到價格 selector |
| 價格文字有逗號或貨幣符號 | 清理後再轉 number |
| 價格為 0 / NaN / Infinity | 不寫入正常快照 |
| job 跑一半失敗 | 狀態標記為 `partial_success` 或 `failed` |
| 重複跑同一天價格 | 可以允許多筆快照，但要保留時間戳 |
| 外部來源很慢 | 設 timeout，避免整個 job 卡死 |

---

## 十九、技術限制與建議套件

### 後端

| 類型 | 建議 |
|---|---|
| Web framework | Express / Fastify / NestJS |
| 排程 | `node-cron` |
| HTTP client | `fetch` / `axios` |
| HTML parser | `cheerio` |
| ORM | Prisma / TypeORM / Sequelize |
| Database | PostgreSQL / MySQL / SQLite |
| Validation | Zod / Joi |
| Chart | Chart.js / ECharts |

### 爬蟲限制

請遵守以下原則：

- 優先使用公開 API 或允許抓取的資料來源
- 不要繞過登入、付費牆、驗證碼或反爬限制
- 設定合理頻率，不要高頻打爆來源網站
- README 要寫清楚資料來源與限制
- 如果真實來源不穩，可以提供 mock source 作為展示備援，但主流程仍要說明真實來源設計

---

## 二十、小組分工建議

| 角色 | 負責內容 |
|---|---|
| PM / Presenter | 梳理需求、設計 Demo 流程、整理成果發表 |
| Backend API | Card / Price / Job API、錯誤處理、權限保護 |
| Data / DB | 資料表設計、ORM schema、migration、seed data |
| Crawler / Integration | API adapter、crawler adapter、價格清洗、timeout |
| Frontend | 卡牌列表、詳情頁、後台頁、趨勢圖 |
| QA / Docs | 測試案例、README、Demo 資料、AI 協作紀錄 |

每位組員成果發表時都要能說出：

- 我負責哪一段流程
- 我遇到什麼問題
- 我如何驗證它真的可用
- 我使用 AI 加速了哪裡
- 最後哪些判斷不是 AI 幫我決定，而是我自己取捨

---

## 二十一、產品用心點

這題的評分重點不是「你抓了多少卡」，而是你們有沒有把產品流程想完整。

可以展示的用心點：

- 卡牌搜尋是否好用
- 最新價格與歷史價格是否容易理解
- 來源失敗時是否有清楚狀態
- job log 是否能幫助 debug
- 資料表是否能支援未來擴充
- 價格異常是否有防呆
- README 是否讓別人真的跑得起來
- Demo 是否能清楚解釋資料從外部來源進到前台的過程

---

## 二十二、加分功能

主流程穩定後，可以選擇 1-2 個加分功能，不要一開始全做。

| 功能 | 說明 |
|---|---|
| Watchlist | 使用者可以收藏關注卡牌 |
| 多來源平均價 | 將多個來源價格做平均或中位數 |
| 7 / 30 天漲跌幅 | 用歷史快照計算趨勢 |
| 匯率換算 | 將 USD / JPY 換算成 TWD |
| 異常價格偵測 | 價格突然變成 0 或大幅跳動時標記 |
| 通知功能 | 關注卡牌價格超過門檻時通知 |
| 匯出 CSV | 匯出卡牌價格歷史 |
| Queue | 使用 BullMQ / Redis 管理大量抓價任務 |
| Playwright crawler | 當來源需要 JS render 時，用 Playwright 抓取 |

---

## 二十三、風險與取捨

| 風險             | 可能影響      | 建議取捨                          |
| -------------- | --------- | ----------------------------- |
| 外部網站改版         | 爬蟲失效      | 用 adapter 包裝，錯誤要能被 log 看見     |
| API rate limit | 更新失敗      | 降低頻率，記錄 429，避免無限重試            |
| 價格來源定義不同       | 價格不可直接比較  | 顯示來源，必要時不要硬算平均                |
| 品相 / 鑑定分數混在一起  | 價格失真      | Card 要記錄 condition / grade    |
| 資料量變大          | 查詢變慢      | MVP 先少量資料，進階再加 index          |
| 排程重複執行         | 重複寫入或資料混亂 | 用 running job 檢查或 job lock    |
| 價格解析錯誤         | 前台顯示荒謬價格  | 對 NaN / Infinity / 0 做防呆      |
| Demo 當天來源失敗    | 主流程無法展示   | 準備 seed data 與 mock source 備援 |

---

## 二十四、30 天開發節奏

| 階段    | 時間        | 交付                                         |
| ----- | --------- | ------------------------------------------ |
| 選題前準備 | 第四週前      | 理解 PRD、研究卡價來源、填寫服務選擇表                      |
| W1    | Day 1-7   | 完成需求拆解、資料表設計、API 草稿、前台 wireframe           |
| W2    | Day 8-14  | 完成卡牌 CRUD、前台列表 / 詳情、seed data、資料庫串接        |
| W3    | Day 15-21 | 完成 API adapter、crawler adapter、手動 job、價格快照 |
| W4    | Day 22-28 | 完成 cron 排程、job log、錯誤處理、趨勢表 / 圖            |
| 收尾    | Day 29-30 | README、測試、Demo 腳本、AI 協作紀錄、成果發表簡報           |

每週都要有交付，不要等最後一週才整合。

---

## 二十五、成果發表需要說明

成果發表不只展示畫面，也要說明你們的開發判斷。

建議簡報順序：

1. 你們做的是什麼服務
2. 使用者為什麼需要它
3. 主流程 Demo：搜尋卡牌 → 看最新價格 → 看歷史價格
4. 管理流程 Demo：新增卡牌 → 手動更新 → 查看 job log
5. 技術流程圖：排程 → adapter → 清洗 → 快照 → 前台
6. 資料表設計：Card / Source / Snapshot / Job
7. 遇到的困難：來源失敗、價格解析、排程、資料異常
8. 你們做了哪些用心與創新
9. 使用哪些 AI 服務加速開發
10. 如果再多一週，你們會先改善什麼

---

## 二十六、AI 協作紀錄

每組需要記錄 AI 參與了哪些部分，成果發表時要分享。

| 面向 | 可以記錄什麼 |
|---|---|
| 需求拆解 | 是否用 AI 幫忙拆使用者故事、API、資料表 |
| 來源研究 | 是否用 AI 比較 API / 爬蟲資料來源 |
| 程式開發 | 是否用 AI 生成 controller、schema、adapter 初稿 |
| Debug | 是否用 AI 分析爬蟲失敗、資料格式錯誤、cron 問題 |
| 測試 | 是否用 AI 產生測試案例或 edge cases |
| README | 是否用 AI 協助整理安裝與 Demo 流程 |
| 判斷與取捨 | 哪些決策是小組自己做的，而不是照 AI 原文使用 |

請誠實記錄，不是用越多 AI 分數越高。重點是你們能不能說清楚 AI 幫了哪裡，以及你們怎麼驗證 AI 給的結果。

---

## 二十七、評分標準

| 面向 | 比重 | 評估重點 |
|---|---:|---|
| 產品理解 | 20% | 是否理解卡價追蹤不是單次查詢，而是持續資料流程 |
| 功能完成度 | 25% | 前台、後台、手動更新、排程更新、歷史價格是否可 Demo |
| 技術設計 | 25% | 資料表、adapter、job log、錯誤處理是否合理 |
| 用心與創新 | 15% | 是否有趨勢理解、異常防呆、使用者體驗打磨 |
| AI 協作能力 | 10% | 是否善用 AI 加速，並能說明驗證與取捨 |
| 表達與 Demo | 5% | 成果發表是否清楚、流暢、有重點 |

---

## 二十八、驗收標準

MVP 完成時，至少要能做到：

- 可以啟動後端服務
- 可以啟動前台畫面
- 資料庫可以建立必要資料表
- 可以建立 3-5 張追蹤卡牌
- 可以設定至少一個 API 來源與一個爬蟲來源
- 可以手動觸發一次價格更新
- 可以用排程自動執行價格更新
- 成功更新時會新增價格快照
- 失敗時會留下 job log 與錯誤訊息
- 前台可以搜尋卡牌
- 前台可以看到最新價格與歷史價格
- README 可以讓別人照步驟跑起來
- 成果發表可以說清楚資料流、困難、取捨與 AI 協作方式

---

## 二十九、建議 Demo 資料

為了讓成果發表穩定，請準備固定的 seed data。

建議至少包含：

- 3 張一般卡牌
- 1 張高價卡牌
- 1 張沒有成功更新過價格的卡牌
- 1 個 API 來源
- 1 個 crawler 來源
- 5-10 筆歷史價格快照
- 1 筆成功 job
- 1 筆失敗 job

這樣即使外部來源當天不穩，還是可以展示完整產品流程。