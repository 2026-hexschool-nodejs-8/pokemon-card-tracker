# 測試專用環境變數（進版控，不含真實密鑰）
# 用法：node --env-file=.env.test --test ...
DATABASE_URL="postgresql://pct:pct_password@localhost:5432/pokemon_card_tracker_test?schema=public"
JWT_SECRET="test-secret-do-not-use-in-prod"
JWT_EXPIRES_IN="1h"
FETCH_TIMEOUT_MS=200
CORS_ORIGIN="http://localhost:5173"
PORT=3001
ENABLE_CRON=false
