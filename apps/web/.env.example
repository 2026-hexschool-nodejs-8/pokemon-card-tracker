# 前端環境變數（Vite 慣例，需 VITE_ 前綴）
# 複製為 .env 後調整：cp .env.example .env

# 後端 API 位址（dev 模式由 vite proxy 使用）
VITE_API_URL=http://localhost:3000

# 正式環境（如 Render Static Site）才設定：後端 API 的完整網址，結尾不要加斜線
# dev 請留空 → api.js 會 fallback 成 /api，繼續走上面的 vite proxy（免 CORS）
# VITE_API_BASE_URL=https://pct-api.onrender.com
